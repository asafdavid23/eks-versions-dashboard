version: '3'

dotenv:
  - .env
  - .env.local

tasks:
  local:
    vars:
      port: '{{ .port | default "8080" }}'
    cmds:
      - echo "Run Vue.js frontend locally"
      - cd vue && npm run dev -- --port {{ .port }}
      - echo "Frontend is running on http://localhost:{{ .port }}"