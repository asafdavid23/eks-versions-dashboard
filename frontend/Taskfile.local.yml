version: '3'

tasks:
  start:
    cmds:
      - echo "Run Vue.js frontend locally"
      - BROWSER=none nohup npm --prefix frontend/vue run dev -- --port 8080 &
      - echo "Frontend is running on http://localhost:8080"
  silent: false